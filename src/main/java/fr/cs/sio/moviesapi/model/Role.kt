package fr.cs.sio.moviesapi.model

/**
 * An enum for the different roles a Person can have.
 * In our model, a role is NOT linked to a specific movie.
 */
enum class Role {

    DIRECTOR,
    ACTOR

}
